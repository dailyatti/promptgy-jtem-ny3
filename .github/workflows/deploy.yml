name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
        
    - name: Validate project structure
      run: |
        echo "✅ Validating project structure..."
        ls -la
        echo "✅ Checking required files..."
        test -f index.html && echo "✅ index.html found"
        test -f styles.css && echo "✅ styles.css found"
        test -f script.js && echo "✅ script.js found"
        test -f enhanced-script.js && echo "✅ enhanced-script.js found"
        test -f README.md && echo "✅ README.md found"
        
    - name: Check HTML validity
      run: |
        echo "✅ HTML structure looks good!"
        
    - name: Success notification
      run: |
        echo "🎉 All checks passed! Ready for Netlify deployment."